//import solidity
pragma solidity ^0.8.12;

contract Simple {
    function calcs(uint _a, unit_b) public pure{
        return(uint o_sum, uint a_product){
            o_sum = _a + _b;
            a_product = _a * _b;
        }
    }
}